# Simulation report example

`baseline_simulation_report.json` is produced by the offline simulation pipeline (`adml simulate` or `python scripts/generate_simulation_example.py`). Event timestamps are omitted in this checked-in copy so regression tests can diff reports deterministically.

The `provenance` block conforms to `evaluation_run_provenance.v1.json`.